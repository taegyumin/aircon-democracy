node_modules
dist
dist-ssr
.DS_Store
*.local
.vite
*.tsbuildinfo

# Next.js / Turbo build artifacts (root에서도 발생 — apps/web/.next 외에)
.next
.turbo
.env
.env.*
!.env.example

# Wrangler / Cloudflare local state (when we add Workers)
.wrangler
.dev.vars

# Playwright test artifacts
playwright-report
test-results
.playwright

# Next.js / Expo build artifacts
apps/web/.next
apps/web/out
apps/mobile/.expo
apps/mobile/dist
apps/web/.vercel

# Capacitor native dirs (legacy, Expo로 대체됨)
android/
ios/
